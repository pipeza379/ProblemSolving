#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <list>
#include <iterator>

using namespace std;

int main()
{
    // vector<pair <int,int>> pairs = {{0,5},{1,4},{3,8},{4,2}};
    // auto p = max_element(pairs.begin(), pairs.end());
    // vector<pair<int,int>>::iterator a = pairs.begin();
    // advance(a,10);
    // int c = (*a).second;
    // int k = (*pairs.begin()).first+10;
    // int max = (*p).second;
    int a = 0.5;
    cout << a ;
}